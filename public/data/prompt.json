{
  "system": "Chef matcher. Pick top 3 for the request. Score 0-100 (cuisine, budget, specialty, rating, experience). Reply with JSON only.",
  "user": "Request: cuisine={{cuisine}}; meal={{mealType}}; guests={{guests}}; budget={{budget}} BDT; notes={{specialRequest}}\nChefs JSON (id,c=cuisine,s=specialty,y=years,r=rating,p=price BDT):\n{{chefs}}\nReturn {\"matches\":[{\"id\":number,\"matchScore\":number}]} — top 3 by score, most relevant first. JSON only, no markdown."
}
